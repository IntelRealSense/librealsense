// License: Apache 2.0. See LICENSE file in root directory.
// Copyright(c) 2022 Intel Corporation. All Rights Reserved.

/*!
 * @file deviceInfoTypeObject.cpp
 * This source file contains the definition of the described types in the IDL file.
 *
 * This file was generated by the tool gen.
 */

#ifdef _WIN32
// Remove linker warning LNK4221 on Visual Studio
namespace { char dummy; }
#endif

#include <librealsense2/dds/topics/device-info/deviceInfo.h>
#include "deviceInfoTypeObject.h"
#include <utility>
#include <sstream>
#include <fastrtps/rtps/common/SerializedPayload.h>
#include <fastrtps/utils/md5.h>
#include <fastrtps/types/TypeObjectFactory.h>
#include <fastrtps/types/TypeNamesGenerator.h>
#include <fastrtps/types/AnnotationParameterValue.h>
#include <fastcdr/FastBuffer.h>
#include <fastcdr/Cdr.h>

using namespace eprosima::fastrtps::rtps;

void registerdeviceInfoTypes()
{
    TypeObjectFactory *factory = TypeObjectFactory::get_instance();
    factory->add_type_object("librealsense::dds::topics::raw::device_info", librealsense::dds::topics::raw::Getdevice_infoIdentifier(true),
            librealsense::dds::topics::raw::Getdevice_infoObject(true));
    factory->add_type_object("librealsense::dds::topics::raw::device_info", librealsense::dds::topics::raw::Getdevice_infoIdentifier(false),
            librealsense::dds::topics::raw::Getdevice_infoObject(false));





}

namespace librealsense {
    namespace dds {
        namespace topics {
            namespace raw {
                const TypeIdentifier* Getdevice_infoIdentifier(bool complete)
                {
                    const TypeIdentifier * c_identifier = TypeObjectFactory::get_instance()->get_type_identifier("device_info", complete);
                    if (c_identifier != nullptr && (!complete || c_identifier->_d() == EK_COMPLETE))
                    {
                        return c_identifier;
                    }

                    Getdevice_infoObject(complete); // Generated inside
                    return TypeObjectFactory::get_instance()->get_type_identifier("device_info", complete);
                }

                const TypeObject* Getdevice_infoObject(bool complete)
                {
                    const TypeObject* c_type_object = TypeObjectFactory::get_instance()->get_type_object("device_info", complete);
                    if (c_type_object != nullptr)
                    {
                        return c_type_object;
                    }
                    else if (complete)
                    {
                        return GetCompletedevice_infoObject();
                    }
                    //else
                    return GetMinimaldevice_infoObject();
                }

                const TypeObject* GetMinimaldevice_infoObject()
                {
                    const TypeObject* c_type_object = TypeObjectFactory::get_instance()->get_type_object("device_info", false);
                    if (c_type_object != nullptr)
                    {
                        return c_type_object;
                    }

                    TypeObject *type_object = new TypeObject();
                    type_object->_d(EK_MINIMAL);
                    type_object->minimal()._d(TK_STRUCTURE);

                    type_object->minimal().struct_type().struct_flags().IS_FINAL(false);
                    type_object->minimal().struct_type().struct_flags().IS_APPENDABLE(false);
                    type_object->minimal().struct_type().struct_flags().IS_MUTABLE(false);
                    type_object->minimal().struct_type().struct_flags().IS_NESTED(false);
                    type_object->minimal().struct_type().struct_flags().IS_AUTOID_HASH(false); // Unsupported

                    MemberId memberId = 0;
                    MinimalStructMember mst_name;
                    mst_name.common().member_id(memberId++);
                    mst_name.common().member_flags().TRY_CONSTRUCT1(false); // Unsupported
                    mst_name.common().member_flags().TRY_CONSTRUCT2(false); // Unsupported
                    mst_name.common().member_flags().IS_EXTERNAL(false); // Unsupported
                    mst_name.common().member_flags().IS_OPTIONAL(false);
                    mst_name.common().member_flags().IS_MUST_UNDERSTAND(false);
                    mst_name.common().member_flags().IS_KEY(false);
                    mst_name.common().member_flags().IS_DEFAULT(false); // Doesn't apply
                    mst_name.common().member_type_id(*TypeObjectFactory::get_instance()->get_array_identifier("char", {128}, false));


                    MD5 name_hash("name");
                    for(int i = 0; i < 4; ++i)
                    {
                        mst_name.detail().name_hash()[i] = name_hash.digest[i];
                    }
                    type_object->minimal().struct_type().member_seq().emplace_back(mst_name);

                    MinimalStructMember mst_serial_number;
                    mst_serial_number.common().member_id(memberId++);
                    mst_serial_number.common().member_flags().TRY_CONSTRUCT1(false); // Unsupported
                    mst_serial_number.common().member_flags().TRY_CONSTRUCT2(false); // Unsupported
                    mst_serial_number.common().member_flags().IS_EXTERNAL(false); // Unsupported
                    mst_serial_number.common().member_flags().IS_OPTIONAL(false);
                    mst_serial_number.common().member_flags().IS_MUST_UNDERSTAND(false);
                    mst_serial_number.common().member_flags().IS_KEY(false);
                    mst_serial_number.common().member_flags().IS_DEFAULT(false); // Doesn't apply
                    mst_serial_number.common().member_type_id(*TypeObjectFactory::get_instance()->get_array_identifier("char", {32}, false));


                    MD5 serial_number_hash("serial_number");
                    for(int i = 0; i < 4; ++i)
                    {
                        mst_serial_number.detail().name_hash()[i] = serial_number_hash.digest[i];
                    }
                    type_object->minimal().struct_type().member_seq().emplace_back(mst_serial_number);

                    MinimalStructMember mst_product_line;
                    mst_product_line.common().member_id(memberId++);
                    mst_product_line.common().member_flags().TRY_CONSTRUCT1(false); // Unsupported
                    mst_product_line.common().member_flags().TRY_CONSTRUCT2(false); // Unsupported
                    mst_product_line.common().member_flags().IS_EXTERNAL(false); // Unsupported
                    mst_product_line.common().member_flags().IS_OPTIONAL(false);
                    mst_product_line.common().member_flags().IS_MUST_UNDERSTAND(false);
                    mst_product_line.common().member_flags().IS_KEY(false);
                    mst_product_line.common().member_flags().IS_DEFAULT(false); // Doesn't apply
                    mst_product_line.common().member_type_id(*TypeObjectFactory::get_instance()->get_array_identifier("char", {16}, false));


                    MD5 product_line_hash("product_line");
                    for(int i = 0; i < 4; ++i)
                    {
                        mst_product_line.detail().name_hash()[i] = product_line_hash.digest[i];
                    }
                    type_object->minimal().struct_type().member_seq().emplace_back(mst_product_line);

                    MinimalStructMember mst_topic_root;
                    mst_topic_root.common().member_id(memberId++);
                    mst_topic_root.common().member_flags().TRY_CONSTRUCT1(false); // Unsupported
                    mst_topic_root.common().member_flags().TRY_CONSTRUCT2(false); // Unsupported
                    mst_topic_root.common().member_flags().IS_EXTERNAL(false); // Unsupported
                    mst_topic_root.common().member_flags().IS_OPTIONAL(false);
                    mst_topic_root.common().member_flags().IS_MUST_UNDERSTAND(false);
                    mst_topic_root.common().member_flags().IS_KEY(false);
                    mst_topic_root.common().member_flags().IS_DEFAULT(false); // Doesn't apply
                    mst_topic_root.common().member_type_id(*TypeObjectFactory::get_instance()->get_array_identifier("char", {256}, false));


                    MD5 topic_root_hash("topic_root");
                    for(int i = 0; i < 4; ++i)
                    {
                        mst_topic_root.detail().name_hash()[i] = topic_root_hash.digest[i];
                    }
                    type_object->minimal().struct_type().member_seq().emplace_back(mst_topic_root);

                    MinimalStructMember mst_locked;
                    mst_locked.common().member_id(memberId++);
                    mst_locked.common().member_flags().TRY_CONSTRUCT1(false); // Unsupported
                    mst_locked.common().member_flags().TRY_CONSTRUCT2(false); // Unsupported
                    mst_locked.common().member_flags().IS_EXTERNAL(false); // Unsupported
                    mst_locked.common().member_flags().IS_OPTIONAL(false);
                    mst_locked.common().member_flags().IS_MUST_UNDERSTAND(false);
                    mst_locked.common().member_flags().IS_KEY(false);
                    mst_locked.common().member_flags().IS_DEFAULT(false); // Doesn't apply
                    mst_locked.common().member_type_id(*TypeObjectFactory::get_instance()->get_type_identifier("bool", false));

                    MD5 locked_hash("locked");
                    for(int i = 0; i < 4; ++i)
                    {
                        mst_locked.detail().name_hash()[i] = locked_hash.digest[i];
                    }
                    type_object->minimal().struct_type().member_seq().emplace_back(mst_locked);


                    // Header
                    // TODO Inheritance
                    //type_object->minimal().struct_type().header().base_type()._d(EK_MINIMAL);
                    //type_object->minimal().struct_type().header().base_type().equivalence_hash()[0..13];

                    TypeIdentifier identifier;
                    identifier._d(EK_MINIMAL);

                    SerializedPayload_t payload(static_cast<uint32_t>(
                        MinimalStructType::getCdrSerializedSize(type_object->minimal().struct_type()) + 4));
                    eprosima::fastcdr::FastBuffer fastbuffer((char*) payload.data, payload.max_size);
                    // Fixed endian (Page 221, EquivalenceHash definition of Extensible and Dynamic Topic Types for DDS document)
                    eprosima::fastcdr::Cdr ser(
                        fastbuffer, eprosima::fastcdr::Cdr::LITTLE_ENDIANNESS,
                        eprosima::fastcdr::Cdr::DDS_CDR); // Object that serializes the data.
                    payload.encapsulation = CDR_LE;

                    type_object->serialize(ser);
                    payload.length = (uint32_t)ser.getSerializedDataLength(); //Get the serialized length
                    MD5 objectHash;
                    objectHash.update((char*)payload.data, payload.length);
                    objectHash.finalize();
                    for(int i = 0; i < 14; ++i)
                    {
                        identifier.equivalence_hash()[i] = objectHash.digest[i];
                    }

                    TypeObjectFactory::get_instance()->add_type_object("device_info", &identifier, type_object);
                    delete type_object;
                    return TypeObjectFactory::get_instance()->get_type_object("device_info", false);
                }

                const TypeObject* GetCompletedevice_infoObject()
                {
                    const TypeObject* c_type_object = TypeObjectFactory::get_instance()->get_type_object("device_info", true);
                    if (c_type_object != nullptr && c_type_object->_d() == EK_COMPLETE)
                    {
                        return c_type_object;
                    }

                    TypeObject *type_object = new TypeObject();
                    type_object->_d(EK_COMPLETE);
                    type_object->complete()._d(TK_STRUCTURE);

                    type_object->complete().struct_type().struct_flags().IS_FINAL(false);
                    type_object->complete().struct_type().struct_flags().IS_APPENDABLE(false);
                    type_object->complete().struct_type().struct_flags().IS_MUTABLE(false);
                    type_object->complete().struct_type().struct_flags().IS_NESTED(false);
                    type_object->complete().struct_type().struct_flags().IS_AUTOID_HASH(false); // Unsupported

                    MemberId memberId = 0;
                    CompleteStructMember cst_name;
                    cst_name.common().member_id(memberId++);
                    cst_name.common().member_flags().TRY_CONSTRUCT1(false); // Unsupported
                    cst_name.common().member_flags().TRY_CONSTRUCT2(false); // Unsupported
                    cst_name.common().member_flags().IS_EXTERNAL(false); // Unsupported
                    cst_name.common().member_flags().IS_OPTIONAL(false);
                    cst_name.common().member_flags().IS_MUST_UNDERSTAND(false);
                    cst_name.common().member_flags().IS_KEY(false);
                    cst_name.common().member_flags().IS_DEFAULT(false); // Doesn't apply
                    cst_name.common().member_type_id(*TypeObjectFactory::get_instance()->get_array_identifier("char", {128}, true));


                    cst_name.detail().name("name");

                    type_object->complete().struct_type().member_seq().emplace_back(cst_name);

                    CompleteStructMember cst_serial_number;
                    cst_serial_number.common().member_id(memberId++);
                    cst_serial_number.common().member_flags().TRY_CONSTRUCT1(false); // Unsupported
                    cst_serial_number.common().member_flags().TRY_CONSTRUCT2(false); // Unsupported
                    cst_serial_number.common().member_flags().IS_EXTERNAL(false); // Unsupported
                    cst_serial_number.common().member_flags().IS_OPTIONAL(false);
                    cst_serial_number.common().member_flags().IS_MUST_UNDERSTAND(false);
                    cst_serial_number.common().member_flags().IS_KEY(false);
                    cst_serial_number.common().member_flags().IS_DEFAULT(false); // Doesn't apply
                    cst_serial_number.common().member_type_id(*TypeObjectFactory::get_instance()->get_array_identifier("char", {32}, true));


                    cst_serial_number.detail().name("serial_number");

                    type_object->complete().struct_type().member_seq().emplace_back(cst_serial_number);

                    CompleteStructMember cst_product_line;
                    cst_product_line.common().member_id(memberId++);
                    cst_product_line.common().member_flags().TRY_CONSTRUCT1(false); // Unsupported
                    cst_product_line.common().member_flags().TRY_CONSTRUCT2(false); // Unsupported
                    cst_product_line.common().member_flags().IS_EXTERNAL(false); // Unsupported
                    cst_product_line.common().member_flags().IS_OPTIONAL(false);
                    cst_product_line.common().member_flags().IS_MUST_UNDERSTAND(false);
                    cst_product_line.common().member_flags().IS_KEY(false);
                    cst_product_line.common().member_flags().IS_DEFAULT(false); // Doesn't apply
                    cst_product_line.common().member_type_id(*TypeObjectFactory::get_instance()->get_array_identifier("char", {16}, true));


                    cst_product_line.detail().name("product_line");

                    type_object->complete().struct_type().member_seq().emplace_back(cst_product_line);

                    CompleteStructMember cst_topic_root;
                    cst_topic_root.common().member_id(memberId++);
                    cst_topic_root.common().member_flags().TRY_CONSTRUCT1(false); // Unsupported
                    cst_topic_root.common().member_flags().TRY_CONSTRUCT2(false); // Unsupported
                    cst_topic_root.common().member_flags().IS_EXTERNAL(false); // Unsupported
                    cst_topic_root.common().member_flags().IS_OPTIONAL(false);
                    cst_topic_root.common().member_flags().IS_MUST_UNDERSTAND(false);
                    cst_topic_root.common().member_flags().IS_KEY(false);
                    cst_topic_root.common().member_flags().IS_DEFAULT(false); // Doesn't apply
                    cst_topic_root.common().member_type_id(*TypeObjectFactory::get_instance()->get_array_identifier("char", {256}, true));


                    cst_topic_root.detail().name("topic_root");

                    type_object->complete().struct_type().member_seq().emplace_back(cst_topic_root);

                    CompleteStructMember cst_locked;
                    cst_locked.common().member_id(memberId++);
                    cst_locked.common().member_flags().TRY_CONSTRUCT1(false); // Unsupported
                    cst_locked.common().member_flags().TRY_CONSTRUCT2(false); // Unsupported
                    cst_locked.common().member_flags().IS_EXTERNAL(false); // Unsupported
                    cst_locked.common().member_flags().IS_OPTIONAL(false);
                    cst_locked.common().member_flags().IS_MUST_UNDERSTAND(false);
                    cst_locked.common().member_flags().IS_KEY(false);
                    cst_locked.common().member_flags().IS_DEFAULT(false); // Doesn't apply
                    cst_locked.common().member_type_id(*TypeObjectFactory::get_instance()->get_type_identifier("bool", false));

                    cst_locked.detail().name("locked");

                    type_object->complete().struct_type().member_seq().emplace_back(cst_locked);


                    // Header
                    type_object->complete().struct_type().header().detail().type_name("device_info");
                    // TODO inheritance


                    TypeIdentifier identifier;
                    identifier._d(EK_COMPLETE);

                    SerializedPayload_t payload(static_cast<uint32_t>(
                        CompleteStructType::getCdrSerializedSize(type_object->complete().struct_type()) + 4));
                    eprosima::fastcdr::FastBuffer fastbuffer((char*) payload.data, payload.max_size);
                    // Fixed endian (Page 221, EquivalenceHash definition of Extensible and Dynamic Topic Types for DDS document)
                    eprosima::fastcdr::Cdr ser(
                        fastbuffer, eprosima::fastcdr::Cdr::LITTLE_ENDIANNESS,
                        eprosima::fastcdr::Cdr::DDS_CDR); // Object that serializes the data.
                    payload.encapsulation = CDR_LE;

                    type_object->serialize(ser);
                    payload.length = (uint32_t)ser.getSerializedDataLength(); //Get the serialized length
                    MD5 objectHash;
                    objectHash.update((char*)payload.data, payload.length);
                    objectHash.finalize();
                    for(int i = 0; i < 14; ++i)
                    {
                        identifier.equivalence_hash()[i] = objectHash.digest[i];
                    }

                    TypeObjectFactory::get_instance()->add_type_object("device_info", &identifier, type_object);
                    delete type_object;
                    return TypeObjectFactory::get_instance()->get_type_object("device_info", true);
                }

            } // namespace raw
        } // namespace topics
    } // namespace dds
} // namespace librealsense